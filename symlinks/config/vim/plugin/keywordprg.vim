" -----------------------------------------------------------------------------
"     - Vim-keywordprg-commnads setup -
"
"     Defines custom commands that can be used as keywordprg and work similar
"     to :help
" -----------------------------------------------------------------------------

let g:vim_keywordprg_commands = {
			\ 'Haredoc': ['haredoc %s', 'hare'],
			\}
